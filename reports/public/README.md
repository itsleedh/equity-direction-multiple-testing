# Verified Public Result Tables

These CSV files are generated from named local summary artifacts by
`scripts/build_public_summary.py`. Their source paths and SHA-256 checksums are
included for auditability.

The source artifacts are not published because they derive from
non-redistributed market data and include multi-gigabyte prediction trees. The
public tables are evidence summaries, not independent reproductions of the
historical experiments.
