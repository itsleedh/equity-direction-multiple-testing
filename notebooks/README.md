# Notebooks

No notebook is required to run the research pipeline. Core implementation,
validation, and reporting live in importable Python modules.

Future notebooks should be limited to analysis or demonstration. Before public
release, clear outputs and verify that no credentials, local paths, licensed raw
data, or misleading selectively chosen results are embedded.
